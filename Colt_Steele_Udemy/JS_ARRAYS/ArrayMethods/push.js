// done by KaptainCS3   17 /02/ 2022

//Crucial concept working with javascript arrays

//built in method for array 'push' insert el at the end of the array

const dayOfWeek = ['Monday', 'Tuesday'];
dayOfWeek.push('Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday');    //updates array content and size && and also take multi values
console.warn(dayOfWeek);    // ! note el are inserted at the end of the array !!