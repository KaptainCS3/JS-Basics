// its a concept for representing values of mathematical operations with are not number
// its consider a number in JS :( 
    //example of NaN
    let result = 0 / 0;
    let num = 1 + NaN;
    console.log(result);  //result is NaN
    console.log(typeof(result)); 
    console.log(num);
    console.log(typeof(num));